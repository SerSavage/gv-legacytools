# Git ignore for combined GV Legacy site
.DS_Store
*.log
.env
node_modules/

# PowerShell scripts (development only)
*.ps1

# Database files - KEEP THIS (needed for static site)
# database/ - REMOVED - we need this folder

# Development files
discord-bot.py
env.example
upload-to-github.sh
DEPLOYMENT.md
GIT-SETUP.md
# Note: requirements.txt is kept (empty) to prevent Render Python build errors