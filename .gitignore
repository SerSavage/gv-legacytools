# Git ignore for combined GV Legacy site
.DS_Store
*.log
.env
node_modules/

# PowerShell scripts (development only)
*.ps1

# Python scripts (development/data processing only - not needed for Render)
*.py
!requirements.txt  # Keep requirements.txt (empty) to prevent Render Python build errors
requirements_ocr.txt

# Database files - KEEP THIS (needed for static site)
# database/ - REMOVED - we need this folder

# Development files
discord-bot.py
env.example
upload-to-github.sh
DEPLOYMENT.md
GIT-SETUP.md
UPDATE_ITEMS_INSTRUCTIONS.md
database/reference_matching.csv